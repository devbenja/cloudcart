apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      # *.cloudcart.local solo resuelve en el host (via /etc/hosts). El pod
      # necesita resolverlos para el issuer de Keycloak (server-side de
      # next-auth); se mapean a la IP del serverlb de k3d (traefik LB).
      hostAliases:
        - ip: 172.18.0.3
          hostnames:
            - cloudcart.local
            - api.cloudcart.local
            - keycloak.cloudcart.local
      containers:
        - name: web
          image: cloudcart/web:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: NEXTAUTH_URL
              valueFrom:
                configMapKeyRef:
                  name: env-config
                  key: NEXTAUTH_URL
            - name: NEXTAUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: NEXTAUTH_SECRET
            - name: KEYCLOAK_ISSUER
              valueFrom:
                configMapKeyRef:
                  name: env-config
                  key: KEYCLOAK_ISSUER
            - name: KEYCLOAK_CLIENT_ID
              valueFrom:
                configMapKeyRef:
                  name: env-config
                  key: KEYCLOAK_CLIENT_ID
            - name: INTERNAL_API_URL
              valueFrom:
                configMapKeyRef:
                  name: env-config
                  key: INTERNAL_API_URL
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
