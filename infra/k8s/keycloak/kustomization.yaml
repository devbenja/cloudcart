apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # ConfigMaps generados desde infra/compose/keycloak (fuente única):
  - realm-configmap.yaml
  - theme-configmap.yaml
